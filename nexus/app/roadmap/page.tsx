import { RoadmapClient } from "@/components/roadmap-client";

export const metadata = {
  title: "Roadmap — Nexus",
  description: "Plan and track long-term goals across initiatives and epics.",
};

export default function RoadmapPage() {
  return <RoadmapClient />;
}
